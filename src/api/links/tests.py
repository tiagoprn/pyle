from django.test import TestCase

# Create your tests here.


# Include tests for the token generation also. Check if on creation of a new token before the previous one expiration, the older one gets invalid.
# If not, search on djangorestframework-jwt documentation if there is a way to revoke a token.
